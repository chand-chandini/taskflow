# TaskFlow · Todo App

A component-based Todo application built with **vanilla JavaScript** — no frameworks, just clean architecture.

## 🚀 Run Locally

1. Clone the repo:
   ```bash
   git clone <your-repo-url>
   cd todo-app